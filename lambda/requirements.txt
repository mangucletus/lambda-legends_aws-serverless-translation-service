# lambda/requirements.txt
# Python dependencies for the AWS Lambda function

# AWS SDK for Python - already included in Lambda runtime for Python 3.9
# boto3>=1.26.0

# Additional dependencies if needed (uncomment and add as required)
# requests>=2.28.0
# python-dateutil>=2.8.0
# urllib3>=1.26.0

# Note: boto3 and botocore are already available in the AWS Lambda Python runtime
# No additional dependencies are required for this basic translation function

# If you need to add custom dependencies, uncomment and specify them below:
# pandas>=1.5.0
# numpy>=1.24.0
# Pillow>=9.0.0