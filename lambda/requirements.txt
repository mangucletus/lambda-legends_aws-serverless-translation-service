# lambda/requirements.txt
# Python dependencies for the AWS Lambda function

# AWS SDK for Python - already included in Lambda runtime for Python 3.9
# boto3>=1.26.0 (already available in Lambda runtime)
# botocore>=1.29.0 (already available in Lambda runtime)

# Additional dependencies if needed for enhanced functionality
requests>=2.28.0
python-dateutil>=2.8.0
urllib3>=1.26.0

# JSON handling and data processing
jsonschema>=4.17.0

# Type hints support
typing-extensions>=4.5.0

# Note: boto3 and botocore are already available in the AWS Lambda Python runtime
# The core translation function doesn't require additional dependencies

# Optional dependencies for enhanced features:
# pandas>=1.5.0  # For data processing if needed
# numpy>=1.24.0  # For numerical operations if needed

# Development and testing dependencies (not needed in production):
# pytest>=7.2.0
# moto>=4.1.0  # For mocking AWS services in tests